import { StyleSheet } from "react-native";

import globalStyles from "../../../global-styles";

const styles = StyleSheet.create({
  title: [globalStyles.label13Medium],
  location: [globalStyles.label16Medium],
});

export default styles;
